## 1. CI Target Contract

- [x] 1.1 Pass canonical example target path, operation, and matching stage to `sst install` in CI.

## 2. Verification

- [x] 2.1 Run the CI-equivalent SST install command and infrastructure release checks.
