# SST
.sst/
sst-env.d.ts

# Local environment overrides
*.local.yml

# Local deployment target files (one per target).
# Committed examples are excluded from the ignore with the ! exception below.
*.target.json
!example.target.json
!example.ephemeral.json