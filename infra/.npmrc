auto-install-peers=true
strict-peer-dependencies=false

# Install-script trust allowlist. SST uses esbuild during installation.
# Any new exception requires review and matching documentation updates.
onlyBuiltDependencies[]=esbuild
