info:
  name: Health Check
  type: http
  seq: 1
  tags:
    - health
    - smoke

http:
  method: GET
  url: "{{apiUrl}}/api/health"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  Returns basic health response for the API service.
