info:
  name: Get Service Audit
  type: http
  seq: 33
  tags:
    - domain:services
    - operation:read
http:
  method: GET
  url: "{{apiUrl}}/api/v1/services/{{serviceId}}/audit"
docs: |-
  Purpose: Read audit events for one service.
  Setup: Set `serviceId` from an existing service.
  Expected result: Returns service audit events.
