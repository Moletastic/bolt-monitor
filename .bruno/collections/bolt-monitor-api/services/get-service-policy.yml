info:
  name: Get Service Escalation Policy
  type: http
  seq: 17
  tags:
    - domain:services
    - operation:read
http:
  method: GET
  url: "{{apiUrl}}/api/v1/services/{{serviceId}}/escalation-policy"
docs: |-
  Purpose: Read the escalation policy assigned to a service.
  Setup: Set `serviceId` from an existing service.
  Expected result: Returns the service policy state.
