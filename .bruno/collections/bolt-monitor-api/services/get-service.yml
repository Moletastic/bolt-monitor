info:
  name: Get Service
  type: http
  seq: 16
  tags:
    - domain:services
    - operation:read
http:
  method: GET
  url: "{{apiUrl}}/api/v1/services/{{serviceId}}"
docs: |-
  Purpose: Read one service.
  Setup: Set `serviceId` from a create or list response.
  Expected result: Returns the selected service.
