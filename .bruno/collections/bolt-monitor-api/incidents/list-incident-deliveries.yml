info:
  name: List Incident Deliveries
  type: http
  tags:
    - domain:incidents
    - operation:read
http:
  method: GET
  url: "{{apiUrl}}/api/v1/incidents/{{incidentId}}/deliveries"
docs: |-
  Purpose: list incident delivery state.
  Setup: operator with default tenant access.
  Expected result: 200 OK with deliveries array.
