{
  "meta": {
    "type": "http",
    "name": "replay-incident-delivery-conflict"
  },
  "tags": ["domain:incidents", "operation:write"],
  "http": {
    "method": "POST",
    "url": "{{baseUrl}}/api/v1/incidents/INC_1/deliveries/dlv_terminal/replay",
    "headers": [
      { "name": "Idempotency-Key", "value": "bruno-replay-1", "enabled": true }
    ]
  },
  "docs": "Purpose: replay conflict under matching key.\nSetup: operator; idempotency key reused with the same payload after first replay.\nExpected result: 200 OK with replayed result."
}
