info:
  name: Get Incident
  type: http
  seq: 13
  tags:
    - incidents
    - get

http:
  method: GET
  url: "{{apiUrl}}/api/v1/incidents/{{incidentId}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  Fetches one incident by `incidentId`.
