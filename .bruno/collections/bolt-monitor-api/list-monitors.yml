info:
  name: List Monitors
  type: http
  seq: 4
  tags:
    - monitors
    - list

http:
  method: GET
  url: "{{apiUrl}}/api/v1/monitors"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  Lists monitor resources for the current tenant context.
