info:
  name: Get Scheduler Config
  type: http
  seq: 17
  tags:
    - admin
    - scheduler

http:
  method: GET
  url: "{{apiUrl}}/api/v1/admin/scheduler-config"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

docs: |-
  Reads global recurring execution control state.
