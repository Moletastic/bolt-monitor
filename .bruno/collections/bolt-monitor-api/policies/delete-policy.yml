info:
  name: Delete Escalation Policy
  type: http
  seq: 13
  tags:
    - domain:policies
    - operation:delete
http:
  method: DELETE
  url: "{{apiUrl}}/api/v1/escalation-policies/{{policyId}}"
docs: |-
  Purpose: Delete an unused escalation policy.
  Setup: Set `policyId`; verify no service references it.
  Expected result: Returns successful deletion.
