info:
  name: List Monitor Incidents
  type: http
  seq: 28
  tags:
    - domain:monitors
    - operation:read
http:
  method: GET
  url: "{{apiUrl}}/api/v1/services/{{serviceId}}/monitors/{{monitorId}}/incidents"
docs: |-
  Purpose: List incidents for one monitor.
  Setup: Set `serviceId` and `monitorId` from existing resources.
  Expected result: Returns monitor incidents.
